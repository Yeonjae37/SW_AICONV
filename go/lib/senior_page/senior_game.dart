import 'package:flutter/material.dart';

class SeniorGame extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("치매 예방 사진기억도우미")),
      body: Center(
        child: Column(
        ),
      ),
    );
  }
}